// Importa createContext para crear un contexto global
import { createContext } from "react";
// Crea el CartContext con un array vacío como estado inicial del carrito
export const CartContext = createContext([]);